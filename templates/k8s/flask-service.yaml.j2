apiVersion: v1
kind: Service
metadata:
  name: {{ flask_service_name }}
  namespace: {{ k8s_namespace }}
spec:
  type: NodePort
  selector:
    app: flask
  ports:
    - name: http
      port: {{ flask_container_port }}
      targetPort: {{ flask_container_port }}
      nodePort: {{ flask_nodeport }}
